Debian `live-build` configuration for the ISO (auto-launching Plasma live
session → `koompi-install`, per docs/ARCHITECTURE.md §6). Not implemented
in Phase 1 — this is a placeholder directory; ISO assembly is Phase 4.
