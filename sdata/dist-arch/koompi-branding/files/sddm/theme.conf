[General]
# KOOMPI SDDM greeter (skeleton). Until the branded koompi QML greeter is
# authored, the interim selector (/etc/sddm.conf.d/10-koompi.conf) points SDDM
# at the stock `breeze` theme, which always renders. When the koompi greeter is
# ready, build it out here and switch Current=koompi in 10-koompi.conf.
Background=/usr/share/backgrounds/koompi/koompi-antlers.jpg
