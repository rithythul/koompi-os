# KOOMPI OS — SDDM configuration drop-in.
# Interim: use the stock `breeze` greeter (safe, always renders) until the
# branded koompi greeter under /usr/share/sddm/themes/koompi is finished. Switch
# Current=koompi once that theme is built.
[Theme]
Current=breeze
